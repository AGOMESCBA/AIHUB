// Rotas de configurações do sistema.
// Autenticação foi migrada para modules/auth/routes.js (Fase 1).
// Novas rotas de configuração serão adicionadas aqui nas próximas fases.

module.exports = function registerRoutes(app, { requireAuth }) {
  // placeholder — rotas serão adicionadas nas próximas fases
};
